# مستند کامل پروژه «دانلودر یوتیوب»

> **نوع پروژه:** اپلیکیشن دسکتاپ (GUI) برای دانلود ویدئو و صوت از یوتیوب
> **تکنولوژی:** Python + PyQt6 + yt-dlp
> **زبان رابط کاربری:** فارسی (راست‌به‌چپ)
> **تاریخ مستندسازی:** ۱۵ سپتامبر ۲۰۲۶

---

## فهرست مطالب

1. [معرفی کلی پروژه](#1-معرفی-کلی-پروژه)
2. [معماری و ساختار پروژه](#2-معماری-و-ساختار-پروژه)
3. [شرح تفصیلی هر فایل](#3-شرح-تفصیلی-هر-فایل)
4. [وابستگی‌ها و محیط اجرا](#4-وابستگی‌ها-و-محیط-اجرا)
5. [نحوه اجرای برنامه](#5-نحوه-اجرای-برنامه)
6. [نقاط قوت برنامه](#6-نقاط-قوت-برنامه)
7. [نقاط ضعف و مشکلات (دسته‌بندی‌شده)](#7-نقاط-ضعف-و-مشکلات)
8. [پیشنهادات بهبود (اولویت‌بندی‌شده)](#8-پیشنهادات-بهبود)
9. [جمع‌بندی](#9-جمع‌بندی)

---

## ۱. معرفی کلی پروژه

این پروژه یک **دانلودر گرافیکی یوتیوب** است که با کتابخانه قدرتمند `yt-dlp` کار می‌کند. کاربر لینک ویدئو را وارد می‌کند، برنامه لیست کامل فرمت‌های موجود (کیفیت‌های مختلف ویدئو و صوت) را استخراج و در یک جدول نمایش می‌دهد، و کاربر می‌تواند فرمت دلخواه (یا ترکیبی مثل `137+140`) را برای دانلود انتخاب کند.

**قابلیت‌های اصلی:**

- استخراج خودکار لینک یوتیوب از کلیپ‌بورد
- استخراج و نمایش لیست فرمت‌ها (کیفیت، FPS، زبان، کدک، بیت‌ریت، حجم)
- فیلتر کردن فرمت‌ها (فقط ویدئو، فقط صدا، ویدئو+صدا، بالاترین کیفیت هر رزولوشن و…)
- دانلود ویدئو با کیفیت انتخابی یا ترکیبی از چند فرمت
- دانلود فقط صدا و استخراج با FFmpeg (mp3 / m4a / opus / flac / wav)
- پشتیبانی از کوکی مرورگر (Chrome, Firefox, Edge, Opera, Brave و…) یا فایل `cookies.txt`
- نمایش پیشرفت زنده (سرعت، زمان باقی‌مانده، حجم) و لاگ لحظه‌ای
- تنظیمات سرعت بالا (دانلود همزمان قطعات، چانک ۱۰ مگابایتی، دانلودر خارجی aria2c)

---

## ۲. معماری و ساختار پروژه

پروژه از یک **معماری نسبتاً ماژولار** با تفکیک مسئولیت‌ها استفاده می‌کند. هر فایل یک نقش مشخص دارد:

```
youtube_downloader/
├── main.py          → نقطه ورود برنامه (entry point)
├── gui.py           → رابط کاربری گرافیکی (PyQt6) + مدیریت thread ها
├── downloader.py    → منطق دانلود و استخراج فرمت‌ها (yt-dlp)
├── cookies.py       → شناسایی مرورگرها و مدیریت فایل کوکی
├── settings.py      → ذخیره/بازیابی تنظیمات کاربر (JSON)
├── cookies.txt      → ⚠️ فایل کوکی واقعی نشست گوگل (ریسک امنیتی!)
├── venv/            → محیط مجازی Python 3.14.6 با وابستگی‌ها
└── __pycache__/     → فایل‌های کامپایل‌شده Python
```

**جریان داده (Data Flow):**

```
کاربر لینک را وارد می‌کند
        │
        ▼
gui.py ──(thread)──► downloader.extract_formats() ──► yt-dlp
        │                                                    │
        │◄─── signals.formats_ready (اطلاعات فرمت‌ها) ────────┘
        ▼
parse_formats() → نمایش در جدول
        │
کاربر فرمت/کیفیت را انتخاب می‌کند
        │
        ▼
gui.py ──(thread)──► downloader.download() ──► yt-dlp + (FFmpeg / aria2c)
        │
        │◄─── signals.progress / log / finished ─────────────┘
        ▼
نمایش پیشرفت زنده + ذخیره در پوشه انتخابی
```

**نکته معماری مهم:** ارتباط بین thread های پس‌زمینه و رابط کاربری به‌درستی از طریق سیگنال‌های Qt (`WorkerSignals`) انجام می‌شود، نه دسترسی مستقیم به ویجت‌ها از داخل thread. این یک الگوی صحیح و امن است.

---

## ۳. شرح تفصیلی هر فایل

### ۳.۱. `main.py` (۸ خط)

نقطه ورود ساده برنامه. فقط تابع `run_app()` را از `gui.py` ایمپورت و اجرا می‌کند.

```python
from gui import run_app

if __name__ == "__main__":
    run_app()
```

### ۳.۲. `settings.py` (۶۱ خط) — کلاس `SettingsManager`

مدیریت تنظیمات برنامه با ذخیره‌سازی JSON در `~/.youtube_downloader/config.json`.

**فیلدهای پیش‌فرض:**
| کلید | مقدار پیش‌فرض | توضیح |
|------|--------------|-------|
| `download_path` | پوشه Downloads | مسیر ذخیره |
| `browser` | `"Chrome"` | مرورگر پیش‌فرض برای کوکی |
| `retries` | `5` | تعداد تلاش مجدد |
| `use_custom_cookie` | `False` | استفاده از فایل کوکی سفارشی |
| `custom_cookie_path` | `""` | مسیر فایل کوکی سفارشی |

**نقاط ضعف این فایل:**
- تابع `get_default_download_path()` هر سه شاخه `if/elif/else` یک مقدار یکسان برمی‌گردانند (کد تکراری و بی‌معنا).
- متد `set()` در هر فراخوانی، فایل JSON را دوباره روی دیسک می‌نویسد (I/O مکرر و غیرضروری).

### ۳.۳. `cookies.py` (۱۳۵ خط) — کلاس `CookieManager`

مسئول شناسایی مرورگرهای نصب‌شده و مدیریت فایل کوکی:

- `detect_installed_browsers()`: مسیرهای پروفایل مرورگرها را در ویندوز/مک/لینوکس بررسی می‌کند.
- `validate_cookie_file()`: اعتبارسنجی ساده فایل `cookies.txt` (بررسی هدر Netscape یا وجود Tab).
- `normalize_cookie_file()`: حذف BOM، تبدیل CRLF به LF، و افزودن هدر Netscape در صورت نبود.

**نقاط ضعف:**
- اعتبارسنجی فایل کوکی بسیار ضعیف است: هر فایل متنی که یک Tab داشته باشد، «معتبر» تشخیص داده می‌شود.
- عملیات `normalize_cookie_file` فایل را به‌صورت درجا (in-place) بازنویسی می‌کند — اگر فایل فقط-خواندنی باشد، خطا می‌گیرد.

### ۳.۴. `downloader.py` (۳۸۶ خط) — کلاس `YouTubeDownloader`

قلب برنامه. تمام منطق دانلود و استخراج اطلاعات با `yt-dlp` اینجا است.

**اجزای کلیدی:**
- ثابت‌های سرعت: `CONCURRENT_FRAGMENTS = 8`، `HTTP_CHUNK_SIZE = 10MB`، `BUFFER_SIZE = 1MB`
- `_build_base_opts()`: ساخت پارامترهای پایه yt-dlp شامل تنظیمات سرعت، User-Agent، کوکی و دانلودر خارجی.
- `extract_formats()`: استخراج اطلاعات فرمت‌ها بدون دانلود.
- `download()`: دانلود واقعی با پشتیبانی از progress hook و postprocessor.
- `parse_formats()`: تبدیل خروجی yt-dlp به لیست دیکشنری قابل نمایش.
- `_format_language()` / `_human_size()`: توابع کمکی برای نمایش خوانا.

**نکات فنی قابل توجه:**
- استفاده از `aria2c` به عنوان دانلودر خارجی اگر نصب باشد (اتصال موازی `-x 8`).
- `player_client` شامل `web`, `web_safari`, `tv_embedded` برای سازگاری با کوکی.
- `http_chunk_size = 10MB` برای دور زدن throttle یوتیوب.

### ۳.۵. `gui.py` (۷۱۱ خط) — کلاس `MainWindow`

بزرگ‌ترین فایل پروژه. تمام رابط کاربری با PyQt6 در این فایل است:

**بخش‌های UI (۵ گروه اصلی):**
1. **لینک ویدئو** — ورودی URL + اعتبارسنجی زنده + دکمه استخراج
2. **کوکی‌ها** — انتخاب بین مرورگر یا فایل cookies.txt
3. **فرمت‌های موجود** — جدول ۱۰ ستونه + فیلتر + انتخاب مستقیم ID
4. **ذخیره‌سازی و دانلود** — مسیر، تلاش مجدد، حالت فقط-صدا، دکمه دانلود/لغو
5. **وضعیت و پیشرفت** — نوار پیشرفت + سرعت/ETA/حجم + لاگ زنده

**مدیریت Thread:**
- `_fetch_worker` و `_download_worker` در thread های جداگانه (daemon) اجرا می‌شوند.
- ارتباط با UI از طریق سیگنال‌های `WorkerSignals` (الگوی صحیح).

---

## ۴. وابستگی‌ها و محیط اجرا

**پایتون:** `3.14.6` (محیط مجازی `venv`)

**وابستگی‌های نصب‌شده (استخراج از `pip freeze`):**

| پکیج | نسخه | نقش |
|------|------|-----|
| `yt-dlp` | 2026.8.19 | موتور دانلود |
| `PyQt6` | 6.11.0 | رابط کاربری |
| `PyQt6-Qt6` | 6.11.2 | کتابخانه Qt |
| `PyQt6_sip` | 13.12.0 | اتصال Python-Qt |
| `pyperclip` | 1.11.0 | خواندن کلیپ‌بورد |
| `pyinstaller` | 6.22.3 | ساخت exe |
| `auto-py-to-exe` | 2.50.1 | GUI برای PyInstaller |
| `pyinstaller-hooks-contrib` | 2026.7 | هوک‌های PyInstaller |

**⚠️ وابستگی خارجی (غیر-Python):**

| ابزار | وضعیت روی سیستم فعلی | نقش |
|-------|---------------------|------|
| `ffmpeg` | ✅ نصب (نسخه 9.0.1) | استخراج صدا + merge ویدئو/صدا |
| `aria2c` | ✅ نصب (نسخه 1.37.0) | دانلود موازی سریع‌تر |

> **نکته مهم:** برنامه **هیچ بررسی‌ای در زمان اجرا** برای وجود `ffmpeg` و `aria2c` انجام نمی‌دهد. اگر کاربری این ابزارها را نداشته باشد، دانلود صدا (mp3) یا merge ویدئو بی‌صدا شکست می‌خورد.

---

## ۵. نحوه اجرای برنامه

```bash
# ۱) فعال‌سازی محیط مجازی
cd C:\Users\Asus\Downloads\Video\youtube_downloader
venv\Scripts\activate

# ۲) اجرای برنامه
python main.py
```

> برای ساخت فایل اجرایی (exe) می‌توانید از `auto-py-to-exe` استفاده کنید:
> ```bash
> auto-py-to-exe
> ```

---

## ۶. نقاط قوت برنامه

| # | نقطه قوت | توضیح |
|---|---------|-------|
| ۱ | **تفکیک مسئولیت‌ها** | جداسازی دانلودر/کوکی/تنظیمات از رابط کاربری |
| ۲ | **مدیریت Thread صحیح** | استفاده از سیگنال‌های Qt به‌جای دسترسی مستقیم به UI |
| ۳ | **پشتیبانی RTL** | رابط کاربری کاملاً راست‌به‌چپ و فارسی |
| ۴ | **تنظیمات سرعت** | دانلود همزمان قطعات + چانک ۱۰MB + aria2c |
| ۵ | **پارس قوی فرمت‌ها** | نمایش دقیق کدک/بیت‌ریت/زبان/حجم در جدول |
| ۶ | **انعطاف انتخاب فرمت** | امکان ترکیب دستی ID ها (مثل `137+140`) |
| ۷ | **نرمال‌سازی کوکی** | حذف BOM و تبدیل CRLF برای سازگاری با yt-dlp |
| ۸ | **پشتیبانی چندمرورگری** | تشخیص خودکار Chrome/Firefox/Edge/Opera/Brave |

---

## ۷. نقاط ضعف و مشکلات (دسته‌بندی‌شده)

### 🚨 ۷.۱. مسائل امنیتی (بحرانی)

| # | مشکل | محل | شدت | توضیح |
|---|------|-----|------|-------|
| ۱ | **فایل `cookies.txt` حاوی کوکی‌های زنده نشست گوگل** | `cookies.txt` | 🔴 بحرانی | این فایل شامل توکن‌های `__Secure-3PSID`، `__Secure-1PSIDTS` و `__Secure-3PSIDCC` است که عملاً **کلید ورود به حساب گوگل/یوتیوب کاربر** هستند. هر کسی این فایل را داشته باشد می‌تواند وارد حساب شود. تاریخ انقضای برخی کوکی‌ها سال ۲۰۲۷ است، یعنی هنوز معتبرند. |
| ۲ | **غیرفعال‌سازی اعتبارسنجی SSL** | `downloader.py:59` | 🔴 بحرانی | `"nocheckcertificate": True` یعنی برنامه گواهی‌های HTTPS را تأیید نمی‌کند. این برنامه را در برابر حمله مرد میانی (MITM) آسیب‌پذیر می‌کند. |
| ۳ | **ذخیره کوکی به‌صورت plaintext** | — | 🟠 بالا | کوکی‌ها بدون هیچ رمزنگاری روی دیسک ذخیره شده‌اند. |

### 🐛 ۷.۲. باگ‌ها و رفتارهای نادرست

| # | مشکل | محل | توضیح |
|---|------|-----|-------|
| ۱ | **Regex اعتبارسنجی URL ناقص** | `gui.py:31-34` | آدرس‌هایی مثل `music.youtube.com`، `youtube-nocookie.com` و برخی فرمت‌های دیگر را رد می‌کند. |
| ۲ | **مرتب‌سازی الفبایی ستون «سایز»** | `gui.py:521` | با وجود ذخیره `size_bytes` در UserRole، مرتب‌سازی به‌صورت رشته‌ای انجام می‌شود (مثلاً "99 MB" قبل از "100 MB"). |
| ۳ | **قطع ناگهانی thread هنگام بستن پنجره** | `gui.py:697` | `closeEvent` دانلود فعال را لغو نمی‌کند و منتظر پایان آن نمی‌ماند. رشته‌های daemon با بستن پنجره قطع می‌شوند و فایل ناقص `.part` باقی می‌ماند. |
| ۴ | **`merge_output_format: "mp4"` اجباری** | `downloader.py:211` | برای ویدئوهای با کدک WebM (VP9/Opus) ادغام در کانتینر mp4 ممکن است شکست بخورد. |
| ۵ | **فیلتر «بالاترین کیفیت» نادرست** | `gui.py:482-490` | از `size_bytes` برای انتخاب بهترین فرمت استفاده می‌کند که برای اکثر فرمت‌های DASH صفر است → انتخاب اشتباه. |
| ۶ | **عنوان طولانی/کاراکتر خاص در نام فایل** | `downloader.py:208` | `%(title)s` بدون محدودیت طول؛ عنوان‌های خیلی طولانی روی ویندوز باعث خطای مسیر می‌شوند. |
| ۷ | **عدم تشخیص پلی‌لیست** | — | آدرس پلی‌لیست `youtube.com/playlist` از regex عبور می‌کند اما UI برای دانلود تکی طراحی شده → رفتار نامشخص. |
| ۸ | **تداخل «فقط صدا» با فرمت ویدیویی** | `gui.py:574-576` | اگر کاربر یک فرمت ویدیویی انتخاب کرده و هم‌زمان «فقط صدا» را فعال کند، رفتار گیج‌کننده است. |
| ۹ | **`current_ydl` عملاً مرده** | `downloader.py:33,254,258` | فقط set/None می‌شود و هیچ استفاده واقعی ندارد. |

### 🏗️ ۷.۳. مشکلات طراحی و معماری

| # | مشکل | توضیح |
|---|------|-------|
| ۱ | **`gui.py` یک فایل ۷۱۱ خطی** | ترکیب UI + منطق + thread مدیریت در یک کلاس monolith — نگهداری سخت. |
| ۲ | **کلاس `_Logger` تکراری** | در `downloader.py` دوبار تعریف شده (خط 165 و 235). باید به یک کلاس ماژول-سطح منتقل شود. |
| ۳ | **نبود `requirements.txt`** | وابستگی‌ها فقط در `venv` هستند و مستند نشده‌اند — بازتولید محیط سخت است. |
| ۴ | **I/O مکرر تنظیمات** | `SettingsManager.set()` در هر تغییر (مثلاً toggle کوکی) فایل JSON را می‌نویسد. |
| ۵ | **لاگ بدون محدودیت** | `QTextEdit` لاگ‌ها را بدون سقف نگه می‌دارد → نشتی حافظه در اجرای طولانی. |
| ۶ | **thread های بدون ردیابی** | اشاره‌گر به اشیاء Thread ذخیره نمی‌شود → امکان لغو/انتظار صحیح وجود ندارد. |
| ۷ | **User-Agent قدیمی** | `Chrome/120` مربوط به سال ۲۰۲۳ است و هاردکد شده. |
| ۸ | **`player_client` شکننده** | `tv_embedded` می‌تواند باعث محدودیت سنی یا کیفیت پایین شود و با تغییرات یوتیوب ناپایدار است. |

### ❌ ۷.۴. قابلیت‌های ناقص/موجود نیست

- پشتیبانی از **پلی‌لیست** و دانلود دسته‌ای
- انتخاب **زیرنویس** (با اینکه ستون «زبان» در جدول نمایش داده می‌شود)
- پشتیبانی از **پراکسی** (مهم برای کاربران ایرانی)
- بررسی **runtime** برای `ffmpeg` و `aria2c`
- **تاریخچه دانلود**
- محدودیت نرخ سرعت (rate limit)
- ذخیره متادیتا / کاور / فصل‌ها (chapters)

### 🧪 ۷.۵. تست و کیفیت کد

- ❌ هیچ تستی وجود ندارد (نه unit test نه integration)
- ❌ بدون type checking (mypy) یا linting (ruff/flake8)
- ❌ بدون logging به فایل (همه لاگ‌ها فقط در UI)
- ❌ بدون `.gitignore` (فایل `cookies.txt` و `venv` و `__pycache__` در صورت commit لو می‌روند)

---

## ۸. پیشنهادات بهبود (اولویت‌بندی‌شده)

### 🔴 اولویت ۱ — اقدامات امنیتی فوری

1. **حذف `cookies.txt` از پروژه** و انتقال به `.gitignore`:
   ```gitignore
   cookies.txt
   venv/
   __pycache__/
   *.pyc
   config.json
   ```
2. **تغییر `nocheckcertificate` به `False`** در `downloader.py:59`.
3. کوکی را به‌صورت رمزنگاری‌شده یا حداقل با هشدار به کاربر ذخیره کنید.

### 🟠 اولویت ۲ — رفع باگ‌های اصلی

1. **بهبود regex اعتبارسنجی URL**:
   ```python
   YOUTUBE_URL_RE = re.compile(
       r"^(https?://)?(www\.|m\.|music\.)?(youtube\.com|youtu\.be|youtube-nocookie\.com)/.+$",
       re.IGNORECASE,
   )
   ```
2. **مرتب‌سازی عددی ستون سایز** با override کردن `__lt__` در `QTableWidgetItem` یا استفاده از `setData` با مقدار عددی.
3. **لغو ایمن دانلود هنگام بستن پنجره** در `closeEvent`:
   ```python
   def closeEvent(self, event):
       if self.is_downloading:
           self.downloader.cancel()
           # منتظر پایان thread (با timeout)
       self._save_settings()
       event.accept()
   ```
4. **تغییر `merge_output_format`** به `"mp4/mkv"` برای سازگاری با WebM.
5. **اصلاح فیلتر «بالاترین کیفیت»** با استفاده از `height` + `fps` + `tbr` به‌جای `size_bytes`.

### 🟡 اولویت ۳ — بهبود معماری

1. **افزودن `requirements.txt`**:
   ```
   yt-dlp>=2026.8.0
   PyQt6>=6.11.0
   pyperclip>=1.11.0
   ```
2. **انتقال `_Logger` تکراری** به یک کلاس ماژول-سطح در `downloader.py`.
3. **تقسیم `gui.py`** به چند فایل/کلاس (مثلاً `widgets/format_table.py`، `widgets/download_panel.py`).
4. **بچ کردن ذخیره تنظیمات** (جمع‌آوری تغییرات و نوشتن یک‌باره در `closeEvent`).
5. **محدود کردن حجم لاگ** در `QTextEdit` (مثلاً نگه‌داشتن فقط ۵۰۰ خط آخر).
6. **ثبت thread ها** و مدیریت صحیح چرخه حیات آنها.
7. **به‌روزرسانی User-Agent** یا حذف هاردکد و استفاده از پیش‌فرض yt-dlp.

### 🟢 اولویت ۴ — افزودن قابلیت‌ها

1. **بررسی ffmpeg/aria2c در startup** و نمایش هشدار واضح در صورت نبود.
2. **پشتیبانی از پراکسی** (به‌دلیل شرایط اینترنت ایران، این قابلیت بسیار ارزشمند است).
3. **انتخاب زیرنویس** با استفاده از `writesubtitles` و `subtitleslangs`.
4. **پشتیبانی از پلی‌لیست** با نمایش لیست و انتخاب تکی/گروهی.
5. **تاریخچه دانلود** در settings.
6. **نمایش thumbnail و اطلاعات بیشتر** (توضیحات، آمار بازدید، لایک).

### 🔵 اولویت ۵ — تست و کیفیت

1. نوشتن تست‌های unit برای `parse_formats`، `CookieManager.validate_cookie_file` و `SettingsManager`.
2. افزودن `ruff` برای linting و `mypy` برای type checking.
3. افزودن logging به فایل (با `logging` استاندارد + `RotatingFileHandler`).
4. افزودن CI ساده با GitHub Actions برای اجرای تست‌ها.

---

## ۹. جمع‌بندی

این پروژه در مجموع **کیفیت خوبی** دارد: ساختار ماژولار، مدیریت صحیح thread با سیگنال‌های Qt، تنظیمات سرعت بهینه، و رابط کاربری فارسی راست‌به‌چپ. با این حال دو مشکل امنیتی **بحرانی** دارد (کوکی‌های زنده در `cookies.txt` و غیرفعال‌سازی SSL) که باید **فوراً** رفع شوند، و چند باگ و بهبود معماری نیز برای پایداری و نگهداری بلندمدت لازم است.

**خلاصه اولویت‌ها:**
1. 🔴 حذف کوکی‌های حساس و فعال‌سازی SSL
2. 🟠 رفع باگ‌های اصلی (URL، مرتب‌سازی، merge، لغو ایمن)
3. 🟡 افزودن requirements.txt و بهبود معماری
4. 🟢 قابلیت‌های جدید (پراکسی، زیرنویس، پلی‌لیست)
5. 🔵 تست و لینتینگ
